import StatsLookup from '../types/StatsLookup';
export declare const playerStats: ({ username, platform }: StatsLookup) => {
    method: string;
    url: string;
};
export declare const operatorStats: ({ username, platform }: StatsLookup) => {
    method: string;
    url: string;
};
export declare const seasonalStats: ({ username, platform }: StatsLookup) => {
    method: string;
    url: string;
};
export declare const weaponStats: ({ username, platform }: StatsLookup) => {
    method: string;
    url: string;
};
export declare const weaponCategoryStats: ({ username, platform }: StatsLookup) => {
    method: string;
    url: string;
};
